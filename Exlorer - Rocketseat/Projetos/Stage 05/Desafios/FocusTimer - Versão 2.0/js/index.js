import { getElement, clickListener } from "./utils.js";
import {
  minutesDisplay,
  secondsDisplay,
  playButton,
  stopButton,
  sumButton,
  subButton,
  forestCard,
  rainCard,
  coffeCard,
  fireplaceCard,
} from "./elements.js";

clickListener(playButton, () => {
/*   alert("HELLO");
 */});